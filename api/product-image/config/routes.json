{
  "routes": [
    {
      "method": "GET",
      "path": "/product-images",
      "handler": "product-image.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/product-images/count",
      "handler": "product-image.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/product-images/:id",
      "handler": "product-image.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/product-images",
      "handler": "product-image.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/product-images/:id",
      "handler": "product-image.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/product-images/:id",
      "handler": "product-image.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
